
// This is where faveTodos will be styled, have star button that works as delete.--sb

// import React from "react";

// const FaveTodo = (todo) => {
       
// Tapping Star button removes todo from list.
  
//   const removeFave = (id) => {
//   const newFaveList = todoList.filter((todo) =>
//     id !== todo.id);
//   setFaveList(newFaveList)
// };
//     return (<li key={todo.id}>{todo.title}</li>
                // <button>filled star icon that will delete faveTodo onClick</button>
// )};

// export default FaveTodo;